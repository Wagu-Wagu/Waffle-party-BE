spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://waguwagu-db.cpwueuwiie8u.ap-northeast-2.rds.amazonaws.com:3306/waffle_dev
    username: waguwagu_server
    password: waguwagu1234!

  jpa:
    hibernate:
      ddl-auto: update
      globally_quoted_identifiers: true
    properties:
      hibernate:
        default_batch_fetch_size: 1000
        format_sql: true
        show_sql: true


springdoc:
  version: '@project.version@'
  api-docs:
    path: /waffle-dev-api-docs
  default-consumes-media-type: application/json
  default-produces-media-type: application/json
  swagger-ui:
    operations-sorter: alpha # UI에서 작업을 알파벳 순서로 정렬한다.
    tags-sorter: alpha # UI에서 태그를 알파벳 순서로 정렬한다.
    path: /swagger-ui.html # UI에 액세스할 수 있는 경로를 /swagger-ui.html로 설정한다.
    disable-swagger-default-url: true # 기본 Swagger URL을 비활성화하고 명시적인 경로 /swagger-ui.html로만 접근하도록 한다.
    display-query-params-without-oauth2: true #OAuth2 없이 쿼리 매개변수를 표시한다.
    doc-expansion: none #UI에서 문서 확장을 비활성화한다.
  paths-to-match: #API 문서화에 포함할 경로 패턴을 설정한다. 여기서는 /api/** 로 시작하는 모든 경로가 문서화된다.
#oauth:
#  kakao:
#    client:
#      secret: 4170c1c4e917e7145025d167becf5471
#    redirect:
#      uri: http://localhost:8080/kakao/callback

cloud:
  aws:
    region: ap-northeast-2
    bucket: waguwagu-bucket
    credentials:
      accessKey: AKIA2UC26T3QNCB54Z6D
      secretKey: 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













